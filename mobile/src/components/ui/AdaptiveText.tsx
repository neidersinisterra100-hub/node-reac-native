// import { Platform, Text, TextProps } from "react-native";

// import { Text as PaperText } from "react-native-paper";

// export default function AdaptiveText(props: TextProps) {
//   if (Platform.OS === "web") {
//     return <Text {...props} />;
//   }

//   return <PaperText {...props} />;
// }

// este archivo no es necesario
